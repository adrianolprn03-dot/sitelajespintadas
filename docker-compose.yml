version: '3.8'

services:
  web:
    build: .
    container_name: site_institucional
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./public/uploads:/app/public/uploads
    env_file:
      - .env.production
